/** NAT to transfer IP **/
public class Router {
    // the packet sent to router will be 1494 + 6 bytes, including 747 characters of payload and header, 6 bytes for sequence number.
    // drop probability  = (0+X) * 0.001
    private static final float probability = ;
}
